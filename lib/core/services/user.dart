/*import 'package:firebase_auth/firebase_auth.dart';
import 'package:mhg/api/firestore.dart';
import 'package:mhg/app/app.locator.dart';
import 'package:mhg/app/app.logger.dart';
import 'package:mhg/core/services/authentication/authentication.dart';*/

/*class UserService {
  final log = getStackedLogger('UserService');
  final FireStoreApi _fireStoreApi = locator<FireStoreApi>();
  final AuthenticationService _authService = locator<AuthenticationService>();

  User? _currentUser;
  User? get currentUser => _currentUser;

  // We have to sync our user account
  Future<void> syncUserAccount() async {
    //final firebaseUserId = _authService.
  }
}*/

import 'package:flutter_test/flutter_test.dart';
//import 'package:stacked_services/stacked_services.dart';

void main() {
  group('UserAccessViewModelTest - ', () {
    group('goToNextScreen() - ', () {
      test('When called, should take the profile to signUp Screen', () {
        /*final navService =
            locator<NavigationService>().navigateTo(Routes.signUpView);*/
        /* var model = MhgBaseViewModel().navService;

        var retailerHomeViewModel = RetailerHomeViewModel();

        when(retailerHomeViewModel.goToNextScreen())
            .thenReturn(model.navigateTo(Routes.signUpView));*/
      });
    });
  });
}
